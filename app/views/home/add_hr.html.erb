<div id="page-content-wrapper">
  <div class="container-fluid">
    <%= form_tag(home_save_hr_path, :method => "POST") do %>
      <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this company from being saved:</h2>

          <ul>
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <div>
        <div class="col-md-6">
          <div class="field">
            <%= label_tag :name %><br>
            <%= text_field_tag "name",'' , class: "form-control" %>
          </div>
        </div>
        <div class="col-md-6">
          <div class="field">
            <%= label_tag :name %><br>
            <%= text_field_tag "name",'' , class: "form-control" %>
          </div>
        </div>
      </div>
      <div class="actions">
        <%= submit_tag "Create", :class => "btn btn-default" %>
      </div>
    <% end %>
  </div>
</div>  
